# Arqueología prehistórica — versión single-file

Esta versión contiene todo el CSS y JavaScript dentro de `index.html`.

Para GitHub Pages: reemplaza el `index.html` del repositorio por este archivo. No hace falta subir carpetas.
